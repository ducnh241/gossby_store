<?php

class Controller_Catalog_Backend_Checkout extends Abstract_Backend_Controller {

    public function __construct() {
        parent::__construct();
        die(); //Remove legacy code
    }

    public function actionFootprintList() {
        die(); //Remove legacy code
    }

    public function actionSearch() {
        die(); //Remove legacy code
    }

    public function actionFootprintView() {
        die(); //Remove legacy code
    }

    public function actionFootprintDelete() {
        die(); //Remove legacy code
    }

    protected function _getFilterConfig($filter_value = null) {
        die(); //Remove legacy code
    }

    protected function _applyListCondition(Model_Catalog_Checkout_Footprint_Collection $collection): void {
        die(); //Remove legacy code
    }

}
